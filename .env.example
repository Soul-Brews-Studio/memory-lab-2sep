# Owner passphrase: unlocks the dashboard at / and approves MCP clients on the OAuth page.
# Generate one locally: openssl rand -base64 32
OWNER_PASSPHRASE=replace-with-a-long-random-passphrase
# Optional static bearer for Claude Code / Codex / curl. Blank = OAuth only.
API_TOKEN=
